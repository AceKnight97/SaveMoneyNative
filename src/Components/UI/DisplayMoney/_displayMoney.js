import {StyleSheet, Text, View} from 'react-native';

const DisplayMoneyStyle = StyleSheet.create({
  mainView: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
  },
  money_text: {
    color: 'gold',
    textAlign: 'center',
    fontSize: 24,
    lineHeight: 32,
    fontWeight: 'bold',
  },
});

export default DisplayMoneyStyle;
